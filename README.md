# Gemini i18n Patcher 🇺🇦

Цей інструмент дозволяє додати українську мову до **оригінальної** [Gemini CLI від Google](https://github.com/google-gemini/gemini-cli).

## Переваги
- **Динамічність**: Вибір мови з'являється у вбудованих налаштуваннях `/settings`.
- **Актуальність**: Ви можете оновлювати оригінальну Gemini CLI через `git pull`, а потім просто заново запускати патчер.

## Як користуватися

1. Клонуйте оригінальний репозиторій Google:
   ```bash
   git clone https://github.com/google-gemini/gemini-cli.git
   ```

2. Клонуйте цей патчер поруч:
   ```bash
   git clone https://github.com/santoni-star/gemini-i18n-patcher.git
   ```

3. Запустіть патч:
   ```bash
   cd gemini-i18n-patcher
   node patcher.js ../gemini-cli
   ```

4. Зберіть та встановіть оновлену версію:
   ```bash
   cd ../gemini-cli
   npm install
   npm run bundle
   sudo npm install -g .
   ```

5. Увімкніть українську в програмі:
   `❯ /settings set locale ua`

## Файли перекладу
Всі переклади знаходяться в `assets/ua.ts`. Ви можете редагувати їх там і заново запускати `patcher.js`.